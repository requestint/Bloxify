'use strict';
var classof = require('core-js-pure/internals/classof');
var hasOwn = require('core-js-pure/internals/has-own-property');
var isPrototypeOf = require('core-js-pure/internals/object-is-prototype-of');
var method = require('core-js-pure/stable/array/virtual/for-each');
require('core-js-pure/modules/web.dom-collections.for-each');

var ArrayPrototype = Array.prototype;

var DOMIterables = {
  DOMTokenList: true,
  NodeList: true
};

module.exports = function (it) {
  var own = it.forEach;
  return it === ArrayPrototype || (isPrototypeOf(ArrayPrototype, it) && own === ArrayPrototype.forEach)
    || hasOwn(DOMIterables, classof(it)) ? method : own;
};
